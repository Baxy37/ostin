[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nft-bot"
version = "1.0.0"
description = "NFT bot for Telegram"
requires-python = ">=3.13,<3.14"
dependencies = [
    "aiogram==3.13.1",
    "fastapi==0.115.6",
    "uvicorn==0.34.0"
]
